- name: limit password reuse on Linux
  shell: |
       cp /etc/pam.d/system-auth  /tmp/system-auth.bak
       echo 'password sufficient pam_unix.so use_authtok md5 shadow remember=8' >> /etc/pam.d/system-auth 
